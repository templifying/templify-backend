<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body { font-family: 'Helvetica Neue', Arial, sans-serif; color: #333; padding: 50px 60px; line-height: 1.7; }
    .header { margin-bottom: 40px; }
    .contact-info { display: flex; justify-content: space-between; align-items: flex-start; margin-bottom: 30px; }
    .applicant-info h1 { font-size: 28px; color: #1e40af; margin-bottom: 5px; }
    .applicant-info p { font-size: 13px; color: #666; }
    .date-info { text-align: right; font-size: 14px; color: #666; }
    .divider { height: 3px; background: linear-gradient(90deg, #1e40af, #3b82f6); margin-bottom: 30px; }
    .employer-info { margin-bottom: 30px; }
    .employer-info p { font-size: 14px; margin-bottom: 3px; }
    .employer-info .company { font-weight: 600; color: #333; }
    .subject { margin-bottom: 25px; }
    .subject p { font-size: 14px; }
    .subject strong { color: #1e40af; }
    .salutation { margin-bottom: 20px; font-size: 15px; }
    .body { margin-bottom: 30px; }
    .body p { font-size: 15px; margin-bottom: 18px; text-align: justify; }
    .body ul { margin: 15px 0 15px 25px; }
    .body li { font-size: 14px; margin-bottom: 8px; }
    .closing { margin-bottom: 50px; font-size: 15px; }
    .signature { }
    .signature-name { font-size: 18px; font-weight: 600; color: #1e40af; margin-bottom: 5px; }
    .signature-title { font-size: 13px; color: #666; }
    .highlight { background: #eff6ff; padding: 15px 20px; border-left: 4px solid #1e40af; margin: 20px 0; border-radius: 0 8px 8px 0; }
    .highlight p { margin: 0; font-size: 14px; font-style: italic; color: #1e40af; }
  </style>
</head>
<body>
  <div class="header">
    <div class="contact-info">
      <div class="applicant-info">
        <h1>{{applicantName}}</h1>
        <p>{{applicantEmail}} | {{applicantPhone}}</p>
        <p>{{applicantLocation}}</p>
        {{#if applicantLinkedIn}}<p>{{applicantLinkedIn}}</p>{{/if}}
      </div>
      <div class="date-info">
        <p>{{date}}</p>
      </div>
    </div>
    <div class="divider"></div>
  </div>

  <div class="employer-info">
    <p class="company">{{hiringManagerName}}</p>
    <p>{{hiringManagerTitle}}</p>
    <p>{{companyName}}</p>
    <p>{{companyAddress}}</p>
  </div>

  <div class="subject">
    <p><strong>Re: Application for {{positionTitle}}</strong></p>
  </div>

  <div class="salutation">
    <p>Dear {{hiringManagerName}},</p>
  </div>

  <div class="body">
    <p>{{openingParagraph}}</p>

    {{#if highlightText}}
    <div class="highlight">
      <p>"{{highlightText}}"</p>
    </div>
    {{/if}}

    <p>{{experienceParagraph}}</p>

    {{#if keyAchievements}}
    <p>Key achievements that demonstrate my qualifications:</p>
    <ul>
      {{#each keyAchievements}}
      <li>{{this}}</li>
      {{/each}}
    </ul>
    {{/if}}

    <p>{{whyCompanyParagraph}}</p>

    <p>{{closingParagraph}}</p>
  </div>

  <div class="closing">
    <p>Sincerely,</p>
  </div>

  <div class="signature">
    <div class="signature-name">{{applicantName}}</div>
    {{#if applicantTitle}}<div class="signature-title">{{applicantTitle}}</div>{{/if}}
  </div>
</body>
</html>
