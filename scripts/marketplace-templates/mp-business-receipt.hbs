<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body { font-family: 'Courier New', monospace; color: #333; padding: 30px; max-width: 350px; margin: 0 auto; }
    .header { text-align: center; padding-bottom: 20px; border-bottom: 2px dashed #333; margin-bottom: 20px; }
    .header h1 { font-size: 20px; margin-bottom: 8px; }
    .header p { font-size: 11px; line-height: 1.5; }
    .receipt-info { display: flex; justify-content: space-between; margin-bottom: 20px; padding-bottom: 15px; border-bottom: 1px dashed #999; }
    .receipt-info span { font-size: 11px; }
    .items { margin-bottom: 20px; }
    .item { display: flex; justify-content: space-between; margin-bottom: 8px; font-size: 12px; }
    .item-name { flex: 1; }
    .item-qty { width: 30px; text-align: center; }
    .item-price { width: 60px; text-align: right; }
    .divider { border-top: 1px dashed #999; margin: 15px 0; }
    .totals .row { display: flex; justify-content: space-between; font-size: 12px; margin-bottom: 6px; }
    .totals .row.total { font-size: 16px; font-weight: bold; border-top: 2px solid #333; padding-top: 10px; margin-top: 10px; }
    .payment { text-align: center; margin: 20px 0; padding: 15px; background: #f5f5f5; }
    .payment p { font-size: 11px; margin-bottom: 4px; }
    .payment strong { font-size: 13px; }
    .footer { text-align: center; margin-top: 20px; padding-top: 20px; border-top: 2px dashed #333; }
    .footer p { font-size: 14px; font-weight: bold; margin-bottom: 10px; }
    .footer .small { font-size: 10px; color: #666; }
    .barcode { text-align: center; margin-top: 20px; font-size: 10px; letter-spacing: 2px; }
  </style>
</head>
<body>
  <div class="header">
    <h1>{{businessName}}</h1>
    <p>{{businessAddress}}</p>
  </div>

  <div class="receipt-info">
    <span>{{date}} {{time}}</span>
    <span>#{{receiptNumber}}</span>
  </div>

  <div class="items">
    {{#each items}}
    <div class="item">
      <span class="item-name">{{name}}</span>
      <span class="item-qty">x{{quantity}}</span>
      <span class="item-price">${{total}}</span>
    </div>
    {{/each}}
  </div>

  <div class="divider"></div>

  <div class="totals">
    <div class="row">
      <span>Subtotal</span>
      <span>${{subtotal}}</span>
    </div>
    <div class="row">
      <span>Tax</span>
      <span>${{tax}}</span>
    </div>
    <div class="row total">
      <span>TOTAL</span>
      <span>${{total}}</span>
    </div>
  </div>

  <div class="payment">
    <p>Payment Method</p>
    <strong>{{paymentMethod}}</strong>
  </div>

  <div class="footer">
    <p>{{thankYouMessage}}</p>
    <p class="small">Please keep this receipt for your records</p>
  </div>

  <div class="barcode">
    ||| |||| ||| |||| ||| ||||
  </div>
</body>
</html>
